name: Live QA

on:
  workflow_run:
    workflows: ["Deploy to GitHub Pages"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: live-qa
  cancel-in-progress: false

jobs:
  live-qa:
    name: Portfolio Live QA
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: live-qa
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: main

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Cache Playwright browsers
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-chromium

      - name: Install Playwright chromium
        run: npx playwright install --with-deps chromium

      - name: Verify ANTHROPIC_API_KEY secret present
        env:
          KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ -z "$KEY" ]; then
            echo "::error::ANTHROPIC_API_KEY secret resolves to empty in live-qa environment scope. Re-set the secret value at https://github.com/${{ github.repository }}/settings/environments — listing the secret name only confirms the name exists, not the value."
            exit 1
          fi
          echo "ANTHROPIC_API_KEY present (length=${#KEY})"

      - name: Wait for GitHub Pages CDN propagation
        run: |
          echo "Waiting for live site to reflect latest deploy..."
          for i in $(seq 1 6); do
            status=$(curl -s -o /dev/null -w "%{http_code}" https://rohitvipin.github.io/)
            if [ "$status" = "200" ]; then
              echo "Site reachable (HTTP $status)"
              break
            fi
            echo "Attempt $i: HTTP $status — retrying in 15s"
            sleep 15
          done
          if [ "$status" != "200" ]; then
            echo "::error::Site unreachable after 6 attempts (last HTTP $status) — aborting QA"
            exit 1
          fi

      - name: Run portfolio-qa skill against live site
        uses: anthropics/claude-code-action@567fe954a4527e81f132d87d1bdbcc94f7737434 # v1
        with:
          github_token: ${{ github.token }}
          prompt: |
            Run /portfolio-qa live. Target: https://rohitvipin.github.io
            Save the full report and all screenshots to .qa-reports/
            At the very top of REPORT.md (first line), write exactly one of:
              STATUS: PASS
              STATUS: FAIL
            Use PASS only if there are zero CRITICAL or HIGH failures. Use FAIL otherwise.
            Exit when REPORT.md is written.
          claude_args: "--allowedTools Bash,Read,Write,mcp__playwright__browser_navigate,mcp__playwright__browser_take_screenshot,mcp__playwright__browser_snapshot,mcp__playwright__browser_resize,mcp__playwright__browser_evaluate,mcp__playwright__browser_console_messages,mcp__playwright__browser_network_requests,mcp__playwright__browser_click,mcp__playwright__browser_press_key,mcp__playwright__browser_wait_for,mcp__playwright__browser_type,mcp__playwright__browser_hover"
          settings: |
            {
              "mcpServers": {
                "playwright": {
                  "command": "npx",
                  "args": ["@playwright/mcp@0.0.70", "--browser", "chromium", "--headless"]
                }
              }
            }
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Fail on QA findings
        if: always()
        run: |
          if [ ! -f ".qa-reports/REPORT.md" ]; then
            echo "::error::REPORT.md not written — QA skill failed to run"
            exit 1
          fi
          if ! grep -q "^STATUS: PASS" .qa-reports/REPORT.md; then
            echo "::error::Live QA failed or STATUS sentinel missing — see uploaded report artifact"
            exit 1
          fi

      - name: Upload live QA report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: live-qa-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: .qa-reports/
          retention-days: 14
